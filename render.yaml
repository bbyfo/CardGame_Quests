services:
  - type: web
    name: cardgame-apps
    env: node
    plan: free
    branch: main
    buildCommand: npm install
    startCommand: npm start
    autoDeploy: true
    # envVars:
    #   - key: DATABASE_URL
    #     sync: false
